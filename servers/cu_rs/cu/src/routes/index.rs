    use actix_web::web::{self, ServiceConfig};

    pub fn index(cfg: &mut ServiceConfig) {
        // cfg.service(
        //     web::resource("/path")
        //         .route(web::get().to(handler))   
        // )
    }