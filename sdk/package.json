{
  "name": "@permaweb/ao-sdk",
  "version": "0.0.1",
  "license": "MIT",
  "sideEffects": false,
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "npm run build:types && npm run build:src",
    "build:src": "node esbuild.js",
    "build:types": "tsc src/index.js --skipLibCheck --declaration --allowJs --emitDeclarationOnly --outDir dist",
    "test": "node --test"
  },
  "dependencies": {
    "@permaweb/hyperbeam-loader": "../loader",
    "hyper-async": "^1.1.2",
    "ramda": "^0.29.0",
    "warp-arbundles": "^1.0.4",
    "zod": "^3.22.2"
  },
  "devDependencies": {
    "esbuild": "^0.19.2",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}
