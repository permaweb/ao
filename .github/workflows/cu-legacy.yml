name: 🤖 Test & Deploy AO Compute Unit

on:
  pull_request:
    branches:
      - main
    paths:
      - "servers/cu-legacy/**"
  push:
    branches:
      - main
    paths:
      - "servers/cu-legacy/**"

defaults:
  run:
    shell: bash

jobs:

  test:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout repo
        uses: actions/checkout@v3

      - name: ⎔ Setup node
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📥 Download deps
        uses: bahmutov/npm-install@v1
        with:
          working-directory: servers/cu-legacy

      - name: ⚡ Run Tests
        working-directory: servers/cu-legacy
        run: |
          npm test
        env:
          CI: true

  deploy:
    runs-on: ubuntu-latest
    # Only deploy on main branch
    if: github.ref == 'refs/heads/main'
    # Uncomment if we ever start running the release job
    # needs: [test, release]
    needs: [test]
    steps:
      - name: ⬇️ Checkout repo
        uses: actions/checkout@v3
        with:
          ref: ${{ needs.bump.outputs.TAG }}

      - name: ⎔ Setup node
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📥 Download deps
        uses: bahmutov/npm-install@v1
        with:
          working-directory: servers/cu-legacy

      - name: 👀 Env
        run: |
          echo "Event name: ${{ github.event_name }}"
          echo "Git ref:    ${{ github.ref }}"
          echo "GH actor:   ${{ github.actor }}"
          echo "SHA:        ${{ github.sha }}"
          VER=`node --version`; echo "Node ver:   $VER"
          VER=`npm --version`; echo "npm ver:    $VER"

      - name: 🚀 Deploy
        working-directory: servers/cu-legacy
        env:
          DEPLOY_HOOK: ${{ secrets.CU_LEGACY_RENDER_DEPLOY_HOOK }}
        run: |
          node deploy.js
