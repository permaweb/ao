import { Warp, ArWallet, CustomSignature, Source, SourceData, BundlerSigner, DataItem, Transaction } from 'warp-contracts';
export declare class SourceImpl implements Source {
    private readonly warp;
    private readonly logger;
    private signature;
    constructor(warp: Warp);
    createSource(sourceData: SourceData, wallet: ArWallet | CustomSignature | BundlerSigner, disableBundling?: boolean): Promise<DataItem | Transaction>;
    saveSource(src: DataItem | Transaction, disableBundling?: boolean): Promise<string>;
    private postSource;
    private createSourceArweave;
    private createSourceBundlr;
}
//# sourceMappingURL=SourceImpl.d.ts.map